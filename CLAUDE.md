# 项目约定

## 输出语言：默认中文

本项目的所有交互一律**默认使用简体中文**，无论用户用什么语言提问。适用范围包括但不限于：

- **思考内容**：thinking / 推理过程、方案权衡、排查思路，全部用中文书写。
- **推理与解释**：为什么这么改、有哪些取舍、风险在哪里，用中文说明。
- **结果与结论**：任务小结、验证结论、待办与后续建议，用中文呈现。
- **计划与提问**：plan mode 的计划正文、向用户确认的问题与选项，用中文。
- **报告与文档**：评测报告、落地文档、README 增补等，用中文撰写。
- **提交信息**：git commit message 用中文（沿用现有 `feat: 中文描述` 风格）。
- **代码注释与日志**：新增注释、docstring、面向用户的日志文案用中文。

## 保持英文的例外

以下内容保持原样，不做翻译，以免破坏可运行性与可检索性：

- 代码标识符：变量名、函数名、类名、文件名、路径。
- 技术专有名词与库名：如 RAG、embedding、rerank、BM25、FAISS、pytest 等（首次出现可加中文注解）。
- 命令行、配置键名、环境变量、JSON/YAML 字段。
- 直接引用的报错原文、日志原文、第三方文档原文。

## 风格要求

- 术语中英混排时保持一致，不要同一概念一会儿中文一会儿英文。
- 结论先行：先给结果，再给理由，避免长篇铺垫。
- 涉及文件时使用可点击的相对路径链接，例如 `[build_law_golden_set.py](scripts/build_law_golden_set.py)`。
