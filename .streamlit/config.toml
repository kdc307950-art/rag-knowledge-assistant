[server]
# 默认只允许本机访问。需要局域网或容器访问时必须显式覆盖，并同时配置 APP_PASSWORD。
address = "127.0.0.1"
headless = true
# 生产式本地运行不需要热重载。关闭源码监视可避免 Streamlit 枚举
# transformers 的可选视觉模块并触发与本项目无关的 torchvision 导入异常。
fileWatcherType = "none"

[browser]
gatherUsageStats = false
